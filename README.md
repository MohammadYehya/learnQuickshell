# learnQuickshell

Set the `.gitconfig` file.\
Create the `.git-credentials` file.\
For `git diff`, set the core pager to `cat` (as default is `less`).\
For `git push`, set the credential helper to `store` (so that future pushes use the credentials provided).\
